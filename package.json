{
  "name": "backend",
  "version": "1.0.0",
  "description": "后台服务",
  "main": "index.ts",
  "scripts": {
    "start": "nodemon --ext js,ts --exec npm run dev",
    "dev": "npm run build && node out/index.js",
    "build": "tsc",
    "sync": "npx ts-node ./src/script/sync.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build  && pm2 restart index"
  },
  "keywords": [
    "server"
  ],
  "author": "weishenggao",
  "license": "ISC",
  "dependencies": {
    "@koa/router": "^10.1.1",
    "@sequelize/core": "^7.0.0-alpha.10",
    "axios": "^0.27.2",
    "koa": "^2.13.4",
    "koa-body": "^5.0.0",
    "koa-logger": "^3.2.1",
    "mysql2": "^2.3.3"
  },
  "devDependencies": {
    "@types/koa": "^2.13.4",
    "@types/koa-logger": "^3.1.2",
    "@types/koa__router": "^8.0.11",
    "nodemon": "^2.0.15",
    "sequelize-cli": "^6.4.1",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
