{
  "name": "backend",
  "version": "1.0.0",
  "description": "后台服务",
  "main": "index.ts",
  "scripts": {
    "dev": "npm run build && node dist/index.js",
    "build": "tsc --outDir ./dist",
    "start": "nodemon --ext js,ts --exec npm run dev",
    "sync": "npx ts-node ./src/script/sync.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "tsc --outDir ./dist && pm2 start ./dist/index.js"
  },
  "keywords": [
    "server"
  ],
  "author": "weishenggao",
  "license": "ISC",
  "dependencies": {
    "@koa/router": "^10.1.1",
    "@sequelize/core": "^7.0.0-alpha.10",
    "koa": "^2.13.4",
    "koa-logger": "^3.2.1",
    "mysql2": "^2.3.3"
  },
  "devDependencies": {
    "@types/koa": "^2.13.4",
    "@types/koa-logger": "^3.1.2",
    "@types/koa__router": "^8.0.11",
    "nodemon": "^2.0.15",
    "sequelize-cli": "^6.4.1",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
